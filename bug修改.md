项	是否存在	依据#1 reactive jammer oracle	不存在	没有 jammer_reactive_beta / used_set / 读取 env.uav_channels 加权 jammer 转移。Markov jammer 只是按 p_trans 抽样：ENV_DDRQN.py (line 769)#2 前/后半段干扰靠 list 奇偶	存在	idx[m][0] % 2 判断前后半段：ENV_DDRQN.py (line 646)#3 uav_jump_count 跨 destination 串味	存在	per-cluster 累计：ENV_DDRQN.py (line 1085)，reward 每条链路复用同一个 jump：ENV_DDRQN.py (line 713)#4 无 episode 终止条件	存在	step() 固定 False：ENV_DDRQN.py (line 1136)#5 CSI / sensing 太干净	部分存在	默认 state 直接给精确 fast-fading CSI 和真实 jammer channel：ENV_DDRQN.py (line 604)。但默认分支没有把所有 other UAV channels 拼进 state，uav_channels/uav_powers 算了但没用。#6 p_trans mode 语义混乱	存在	mode=1..4 随机尖峰、mode=3 pass：ENV_DDRQN.py (line 1092)#7 UAV/jammer 运动噪声系数不一致	存在	UAV 用 sqrt(1-k²)：ENV_DDRQN.py (line 870)，jammer 用 sqrt(1-k)：ENV_DDRQN.py (line 1031)#8 __init__ 里 reset 反推 state_dim	存在	构造时直接 self.reset(...) 后 len(self.get_state()[0])：ENV_DDRQN.py (line 382)#9 gym.Env API 不规范	存在/旧 gym 语境下部分存在	继承 gym.Env，但 action_space/observation_space 是 list，reset(p_trans) 非标准，step 返回 4-tuple。旧 gym 允许 4-tuple，但整体仍是误导性继承。#10 运动历史 list 无界增长	存在	每步 append，并用全量 np.average：ENV_DDRQN.py (line 879)#11 全局 RNG	存在	random.sample/random.uniform/random.choices 和 np.random.* 全局混用很多处，例如：ENV_DDRQN.py (line 354)#12 pathloss 双层 Python 循环	存在	channel 类内联在 env 文件里，update_pathloss 双层 loop：ENV_DDRQN.py (line 35)#13 死代码/命名不一致	部分存在	self.channels、self.training 基本无用；h_bs/fc/BS_position 等字段未进入当前 pathloss 公式。但 deepcopy 在这里是用到的，不算死代码。